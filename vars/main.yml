jdk_version: "8" 
jdk_distribution: "jdk"

mysql_databases:
 - name: knowage
   encoding: utf8
 
mysql_users:
 - name: knowage
   host: localhost
   priv: 'knowage.*:ALL'

preend_certbot: True

docker_applications:
  - phpmyadmin

mysql_root_password: "123456"
init_db: 
  mysql:
    admin: root
    password: "123456"
